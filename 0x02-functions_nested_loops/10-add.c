#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @int: The data type
 * Return: (result)
 */
int add(int, int)
{
	int num_1, num_2, result;

	result = num_1 + num_2;
	return (result);
}
