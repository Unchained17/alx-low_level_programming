#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer
 * @j: local variable (parameters)
 * Return: (0)
 */
int _abs(int j)
{
	if (j >= 0)
		return (j);
	else
		return (-j);
}
